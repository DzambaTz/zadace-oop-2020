#include "my.hpp"
#include <iostream>
#include <list>
#include <vector>

// Ovaj main.cpp je samo koristen za rucno "manuelno" testiranje
// odredjenih test case-ova iz file-a test.cpp

int main() {
  /* std::vector<int> v{3, 44, 38, 5, 47, 15, 36, 26, 27, 2, 46, 4, 19, 50, 48};
   */
  /* std::list<int> v{3, 44, 38, 5, 47, 15, 36, 26, 27, 2, 46, 4, 19, 50, 48};
   */
  /* std::list<int> v{0, 10, -8, 1, 18, -8}; */
  /* my::sort(begin(v), end(v), */
  /*         [](const auto &a, const auto &b) { return a > b; }); */
  /* my::sort(v.begin(), v.end(), */
  /*       [](const int &a, const int &b) { return a < b; }); */
  /* std::vector<std::string> v{"ako", "p", "onda", "q"}; */
  /* std::vector<std::string> s{"onda", "ako", "p", "q"}; */
  /* my::sort(begin(v), end(v), */
  /*          [](const auto &a, const auto &b) { return a.size() >= b.size();
   * }); */
  /* for (auto &e : v) { */
  /*   std::cout << e << std::endl; */
  /* } */
  return 0;
}
