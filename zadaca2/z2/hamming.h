#pragma once
#include <string>
#include <vector>
std::vector<std::string> sameLength(std::vector<std::string> &, size_t);
std::vector<std::string> similarStrings(const std::vector<std::string> &,
                                        std::string &);
